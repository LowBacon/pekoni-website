/** Shared between the client hints and the server-side schemas. */
export const USERNAME_PATTERN = /^[A-Za-z0-9_]{3,16}$/;

export const USERNAME_HINT = "3–16 merkkiä: kirjaimia, numeroita ja alaviivoja.";
